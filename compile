#! /bin/bash

set +e

d=`pwd`/tmp/usr
cd binutils-2.20.1/

CFLAGS="-g -fPIC" ./configure --enable-shared --enable-plugins --prefix="$d"
make -j3
make install

cd $d/lib/
ln -s libbfd-2.20.1.20100303.so libbfd-d.so
